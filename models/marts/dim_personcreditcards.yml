version: 2

models:
    - name: dim_personcreditcards
      columns:
            - name: personcreditcard_sk
              description: The surrogate key of person credit cards table
              tests:
                - unique
                - not_null
            - name: businessentityid
            - name: creditcardid